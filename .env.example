# This is an example file. Copy this to .env and fill in your actual values.

# Prisma - PostgreSQL Database
# Your main database connection string.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Prisma - Direct Connection
# Direct connection string, often used for migrations in serverless environments.
# This should point to the same database as DATABASE_URL.
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# NextAuth.js
# A secret used to sign cookies and tokens. You can generate one using: `openssl rand -base64 32`
NEXTAUTH_SECRET="your-nextauth-secret-here"

# Application URL
# The public URL of your deployed application.
NEXT_PUBLIC_APP_URL="http://localhost:3000"